/*
les 4 bases standarts
bases numériques : décimale     ( 10 -> 0 à 9 ) = base qu'on utilise tous les jours
                   binaire      ( 2 - > 0 à 1 ) = base qu'utilise l'ordinateur
                   octale       ( 8 -> 0 à 7 )
                   hexadécimale (16 -> 0 à F)

nombre entier : 14
                18_666
                100_000_000_000 
                ==> underscore pour séparer les nombres quand ils sont trop longs

binaire ( exemple ) -> 0b11001111 -> rajouter le préfixe 0b devant
octal -> 0755 -> préfixe de 0 devant
hexadécimal -> 0xAF55 -> préfixe de 0x devant

caracteres spéciaux et communs à tous les langages :
    \n : nouvelle ligne
    \t : tabulation
    \r : retour chariot
    \b : retour arriere
    \f : nouvelle page
    \' \" : un peu comme les backticks
    \077 \xAF : decimal et hexadecimal
*/ 

public class TypeDeDonnees02 {
  public static void main(String[] args) {
    // Liste des types de données
    // boolean 
    // byte ( de -128 à 127 )
    // char ( comme character )
    // short ( de -32768 à 32767 )
    // int ( comme integer )
    // long ( chiffre vraiment tres long )
    // float ( un décimal jusqu'à puissance 38 )
    // double ( un décimal jusqu'à puissance 320)
    // chaque type de données utilise plus ou moins de place en mémoire

    System.out.println("chapitre suivant");
  }
}
